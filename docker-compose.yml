services:
  edge-tts-blog:
    image: ghcr.io/cs0663c/edge-tts-blog:latest
    container_name: edge-tts-blog
    ports:
      - "2868:8000"      # 宿主机 2868 → 容器内 8000
    volumes:
      - ./examples:/app/examples   # 示例文本目录映射到宿主机，可直接编辑
    restart: unless-stopped
